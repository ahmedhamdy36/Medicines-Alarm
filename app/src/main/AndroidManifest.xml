<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.medicinesalarm">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.android.alarm.permission.SET ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Update"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".my_profile" />
        <activity android:name=".confirm_password" />
        <activity android:name=".verification_forget" />
        <activity android:name=".forget_password" />
        <activity android:name=".waiting2" />
        <activity android:name=".verification" />
        <activity android:name=".registration" />
        <activity android:name=".sittings" />
        <activity android:name=".about" />
        <activity android:name=".add_medicine" />
        <activity android:name=".list_tool" />
        <activity android:name=".waiting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>